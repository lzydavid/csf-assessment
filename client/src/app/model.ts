export interface Upload{
    bundleID:string
    date:string
    title:string
    name:string
    comments:string
    urls:string[]
}

export interface BundleId{
    bundleId:string
}

export interface Bundle {
    title:string
    date:string
}